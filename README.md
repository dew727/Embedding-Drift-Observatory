# Embedding-Drift-Observatory
Build a web platform where users upload a dataset and train an embedding model (e.g., PCA, autoencoder). The system simulates real-world deployment by introducing distribution shift and noise, then analyzes how the learned vector representations (embeddings) evolve over time.
